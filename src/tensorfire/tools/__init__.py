"""Tensorfire tool packs.

Every module here (except those starting with ``_``) is auto-discovered by
``tensorfire.registry`` and must expose ``register(mcp) -> PackInfo``.
See ``_base.py`` for the conventions and ``prompt_injection.py`` for the
simplest working example to copy when adding a new pack.
"""
